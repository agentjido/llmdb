{
  "name": "llmdb-monorepo",
  "private": true,
  "packageManager": "npm@11.19.0",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "npm:build": "npm run build --workspace @agentjido/llmdb",
    "npm:check": "npm run check --workspace @agentjido/llmdb",
    "npm:pack": "npm run pack:check --workspace @agentjido/llmdb",
    "npm:sync": "npm run sync --workspace @agentjido/llmdb",
    "npm:sync:check": "npm run sync:check --workspace @agentjido/llmdb",
    "npm:test": "npm run test --workspace @agentjido/llmdb",
    "npm:typecheck": "npm run typecheck --workspace @agentjido/llmdb"
  },
  "devDependencies": {
    "@types/node": "^24.10.0",
    "typescript": "^5.9.3"
  }
}
